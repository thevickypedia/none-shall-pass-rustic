name: Build, test and store compiled exec as GH asset

on:
  push:
    branches: [ "main" ]

env:
  CARGO_TERM_COLOR: always

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Get Package Name
      run: |
        name=$(cargo metadata --no-deps --format-version 1 | jq -r '.packages[0].name')
        echo "Package Name: $name"
        echo "pkg_name=$name" >> $GITHUB_ENV
      shell: bash
    - name: Set Release Flag
      run: |
        current_version=$(cargo metadata --no-deps --format-version 1 | jq -r '.packages[0].version')
        latest_version=$(curl -s https://crates.io/api/v1/crates/${{ env.pkg_name }} | jq -r '.versions[0].num')
        echo "Current Package Version: ${current_version}"
        echo "Latest Package Version: ${latest_version}"
        if [ "$latest_version" != "$current_version" ]; then
          echo "Version has changed. Setting release flag to true."
          echo "release=true" >> $GITHUB_ENV
        else
          echo "Version has not changed. Setting release flag to false."
          echo "release=false" >> $GITHUB_ENV
        fi
        echo "pkg_version=$current_version" >> $GITHUB_ENV
      shell: bash
    - name: Build
      run: |
        if [ "${{ env.release }}" == "true" ]; then
          cargo build --release
        else
          cargo build --verbose
        fi
    - name: Run tests
      run: |
        if [ "${{ env.release }}" == "true" ]; then
          cargo test
        else
          cargo test --verbose
        fi
    - name: Set commit message
      if: env.release == 'true'
      run: |
        commit_notes=$(git log -1 --pretty=%B | tr -d '`')
        trimmed=$(echo "$commit_notes" | tr -s '[:space:]' | sed ':a;N;$!ba;s/\n/\\n/g')
        echo "commit_msg=$trimmed" >> $GITHUB_ENV
      shell: bash
    - name: Copy Compiled Executable
      if: env.release == 'true'
      run: |
        src_dir="target/release"
        if [ -f "$src_dir/${{ env.pkg_name }}" ]; then
          cp "$src_dir/${{ env.pkg_name }}" ${{ env.pkg_name }}
          echo "Copied ${{ env.pkg_name }} to $(pwd)"
        else
          echo "The file ${{ env.pkg_name }} does not exist in $src_dir."
        fi
      shell: bash
    - name: Create New Release
      if: env.release == 'true'
      # todo: Set prerelease to false in release_data
      run: |
        release_tag="v${{ env.pkg_version }}"
        release_data="{\"tag_name\":\"$release_tag\",\"name\":\"$release_tag\",\"body\":\"${{ env.commit_msg }}\",\"draft\":false,\"prerelease\":true}"
        response=$(curl -X POST -H "Authorization: token ${{ secrets.GIT_TOKEN }}" \
          -d "$release_data" \
          "https://api.github.com/repos/${{ github.repository }}/releases")

        release_id=$(echo $response | jq -r .id)
        echo "Release ID: $release_id"
        echo "release_id=$release_id" >> $GITHUB_ENV
      shell: bash
    - name: Upload Asset to Release
      if: env.release == 'true'
      run: |
        curl -X POST -H "Authorization: token ${{ secrets.GIT_TOKEN }}" \
        -H "Content-Type: application/octet-stream" \
        --data-binary @"${{ env.pkg_name }}" \
        "https://uploads.github.com/repos/${{ github.repository }}/releases/${{ env.release_id }}/assets?name=${{ env.pkg_name }}"
      shell: bash
    - name: Release Crate
      if: env.release == 'true'
      run: |
        cargo login ${{ env.CRATES_TOKEN }}
        cargo publish
      shell: bash
